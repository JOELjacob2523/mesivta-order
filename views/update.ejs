<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="/static/css/update.css">
    <title>Update</title>
</head>
<body>
    <div class="go-back">
        <p class="back-p">Click here to go back to previous page</p>
        <form action="/main" method="GET">
            <input type="submit" value="Go Back" class="back">
        </form>
    </div>
    <div class="create-update">
        <div class="text-center">
            <h3 class="create-h3">Update Vendor</h3>
            <div>
            <% for (let product of products){ %>
            <form action="/update" method="POST">
                <input type="hidden" value="<%= product.productId %>" name="productId">
                <input type="text" value="<%= product.description %>" name="description" class="details-desc">
                <input type="text" value="<%= product.perCase %>" name="perCase" class="details-desc">
                <input type="text" value="<%= product.price %>" name="price" class="details-desc">
                <input type="submit" class="submit" value="Save">
            </form>
            <% } %>
        </div>
      </div>
    </div>
</body>
</html>